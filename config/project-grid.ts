import { ReactElement } from "react";

export interface ProjectGridConfig {
  title: string;
  description: string;
  organisation: string;
  header: ReactElement;
  icon: ReactElement;
  colour: string;
}
